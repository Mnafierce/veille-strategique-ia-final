git remote add origin https://github.com/Mnafierce/veille-strategique-ia-final

# Branche principale
git branch -M main

# Push initial
git push -u origin main

# Pour pousser les fichiers mis à jour ensuite :
git add .
git commit -m "Mise à jour"
git push

# Si un assistant statique est utilisé
# Sauvegarder son ID dans un fichier local ou .env :
# ASSISTANT_ID=asst_123abc...

# Utiliser cet ID dans ton app Streamlit :
# from openai import OpenAI
# client = OpenAI()
# assistant_id = os.getenv("ASSISTANT_ID")

# Ajouter un onglet "🧑‍🏫 Assistant IA" dans app.py avec :
# if selected_tab == "🧑‍🏫 Assistant IA":
#   lancer_thread_stream_openai(assistant_id)

# Cela permet de garder un seul assistant centralisé réutilisable
